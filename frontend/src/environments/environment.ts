// Development environment configuration.
// Used automatically by `ng serve` / `ng build` (default configuration).
// Never hardcode this URL inside components or services - always import
// it from here so the API base can be changed in one place.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api',
};
